# 05. 디자인 가이드라인 (디자인 시스템)

**파일 경로:** `_docs/05_design-guide.md`
**문서 버전:** Ver 1.0
**선행 문서:** `04_wireframe/00~02`

## 1. 디자인 원칙

본 시스템의 디자인은 세 가지 원칙을 따른다. 첫째, **신뢰감**이다. AI가 고가의 PC를 추천하는 서비스이므로 정돈되고 전문적인 인상을 준다. 둘째, **명확한 위계**다. 제목·부제목·본문·수치가 크기와 굵기로 즉시 구분되어 정보를 빠르게 파악하게 한다. 셋째, **한국어 우선**이다. 모든 UI 텍스트는 한국어로 통일하고 기술 약어(CPU, NVMe 등)만 영문을 허용한다.

## 2. 디자인 토큰 (CSS 변수)

Stitch와 Codex는 아래 변수만 사용하며 임의의 색상·크기를 쓰지 않는다.

```css
:root {
  /* ── 컬러 ── */
  --color-primary: #0075d5;        /* 강조·활성·CTA·수치 */
  --color-primary-hover: #005fae;  /* 버튼 hover */
  --color-primary-light: #e6f2fc;  /* 활성 배경·선택 칩 */
  --color-error: #D32F2F;          /* 호환성 위험·경고 */
  --color-success: #2e7d32;        /* 정상·온라인 */
  --color-warning: #f9a825;        /* 진행중·주의 */

  --color-text-strong: #1a1a1a;    /* 제목 */
  --color-text-body: #333333;      /* 본문 */
  --color-text-sub: #888888;       /* 보조·캡션 */
  --color-line: #e0e0e0;           /* 구분선·테두리 */
  --color-bg: #f7f9fc;             /* 페이지 배경 */
  --color-surface: #ffffff;        /* 카드·패널 배경 */

  /* ── 타이포 크기 ── */
  --fs-display: 32px;  --fw-display: 700;
  --fs-h1: 28px;       --fw-h1: 700;
  --fs-h2: 22px;       --fw-h2: 600;
  --fs-h3: 18px;       --fw-h3: 600;
  --fs-body-l: 16px;   --fw-body-l: 400;
  --fs-body: 14px;     --fw-body: 400;
  --fs-caption: 12px;  --fw-caption: 400;
  --fs-button: 15px;   --fw-button: 600;
  --fs-number: 24px;   --fw-number: 700;
  --lh-base: 1.6;      /* 본문 행간 */

  /* ── 폰트 ── */
  --font-family: 'Pretendard', 'Noto Sans KR', system-ui, sans-serif;

  /* ── 간격(8px 그리드) ── */
  --space-xs: 4px;  --space-sm: 8px;  --space-md: 16px;
  --space-lg: 24px; --space-xl: 32px; --space-2xl: 48px;

  /* ── 모서리·그림자 ── */
  --radius-sm: 6px;  --radius-md: 10px;  --radius-lg: 16px;
  --shadow-card: 0 2px 8px rgba(0,0,0,0.06);
  --shadow-hover: 0 6px 20px rgba(0,0,0,0.12);

  /* ── 레이아웃 ── */
  --gnb-height: 64px;
  --lnb-width: 200px;
  --content-max: 1200px;
}
```

## 3. 타이포그래피 적용 규칙

각 토큰의 용도는 와이어프레임과 일치한다. Display(32px Bold)는 랜딩 히어로 카피에만 화면당 1회 사용한다. H1(28px Bold)은 각 화면의 대표 제목에 1개만 쓴다. H2(22px SemiBold)는 섹션 제목, H3(18px SemiBold)는 카드·그룹 제목에 쓴다. 본문은 주요 설명에 Body-L(16px), 일반 텍스트에 Body(14px)를 쓰고 행간은 1.6으로 통일한다. 가격·점수·FPS 같은 수치는 Number(24px Bold)로 Primary 색을 입혀 시각적으로 돋보이게 한다. 도움말·각주·단계 라벨은 Caption(12px) 회색을 쓴다.

## 4. 컴포넌트 규격

### 4.1 버튼
| 종류 | 배경 | 글자 | 용도 |
|------|------|------|------|
| Primary | `--color-primary` | 흰색 | 주요 행동(추천받기·저장·다음) |
| Secondary | 흰색 + Primary 테두리 | Primary | 보조 행동(이전·취소) |
| Disabled | `#cccccc` | `#888888` | 호환성 Error 시 장바구니/결제 잠금 |
| Danger | `--color-error` | 흰색 | 차단·삭제 |

버튼 공통 규격은 높이 44px, 좌우 패딩 24px, 모서리 `--radius-sm`, 글자 `--fs-button(15px) SemiBold`, hover 시 색상 진하게 + 0.2s ease 전환이다.

### 4.2 카드
카드는 `--color-surface` 배경, `--radius-md` 모서리, `--shadow-card` 그림자, 내부 패딩 `--space-lg`를 기본으로 한다. 랜딩의 모드 분기 카드는 hover 시 `--color-primary` 테두리, Scale 1.02, `--shadow-hover`로 변하며 0.2s ease-in-out 전환한다(요구사항 FR-LND-060).

### 4.3 선택 칩(Chip)
용도·예산·감성 옵션에 쓰는 칩은 기본 상태가 흰 배경 + `--color-line` 테두리이고, 선택 시 `--color-primary-light` 배경 + `--color-primary` 테두리·글자로 바뀐다. 다중 선택 칩은 선택된 항목에 체크 아이콘을 추가한다.

### 4.4 입력 요소
텍스트 입력·셀렉트는 높이 40px, `--color-line` 테두리, `--radius-sm`, 포커스 시 Primary 테두리로 강조한다. 슬라이더(예산·전력 마진)는 트랙을 `--color-line`, 채워진 부분과 핸들을 `--color-primary`로 한다. 프롬프트 빌더의 대괄호 키워드는 `--color-primary` 굵게 표시하고 치환 시 0.3s 페이드 애니메이션을 준다.

### 4.5 상태 인디케이터
호환성·인프라 상태는 색 점(●)으로 표시한다. 정상/온라인은 `--color-success`, 진행중은 `--color-warning`, 위험/오프라인은 `--color-error`를 쓴다. 진척도 상태는 대기(`⚪` 회색) → 코딩중(`🟡` warning) → 로컬검증(`🔵` primary) → 배포완료(`●` success)로 통일한다.

## 5. 차트 색상 규격

사용자 결과 화면의 성능/가격 차트는 기존과 동일하게 Chart.js 기준을 유지한다.  
관리자 V3.0 화면은 비즈니스·마케팅 지표의 실시간 대시보드 표현을 위해 Recharts 기반 차트 사용을 허용한다.

차트 색상은 정보 종류별로 고정해 화면 간 일관성을 유지한다.

| 차트 종류 | 기준 라이브러리 | 색상 기준 |
|----------|----------------|----------|
| 사용자 FPS 막대그래프 | Chart.js | Primary `#0075d5` |
| 사용자 가격 원형도표 | Chart.js | CPU/GPU `#0075d5`, 메모리/저장장치 `#4db6ac`, 기타 `#ffb74d` |
| 관리자 특가 CTR 바 차트 | Recharts | Primary `#0075d5` |
| 관리자 키워드 트렌드 | Recharts 또는 워드클라우드 | Primary 계열 + 보조 회색 |
| 관리자 가격동향 라인 차트 | Recharts | 공급가 `#4db6ac`, 판매가 `#0075d5`, 클릭분포 `#ffb74d` |
| 관리자 AI 비용 미니 위젯 | Recharts 또는 숫자 카드 | Gemini `#4285f4`, ChatGPT `#10a37f`, Claude `#d97757` |
| 전환 퍼널 | Recharts | Primary 단색 명도 단계 |


## 6. 레이아웃 규격

사용자 화면은 GNB(높이 64px, sticky 고정) + 콘텐츠(최대 폭 1200px, 중앙 정렬) + 푸터 구조다. 초급자·고급자 입력 화면은 좌측 입력 패널 60% + 우측 프롬프트 패널 40%로 분할하며, 우측 패널은 스크롤 시 화면에 고정(sticky)된다. 관리자 화면은 좌측 LNB(폭 200px 고정) + 우측 콘텐츠 구조다. 모든 영역 간 간격은 8px 그리드(`--space-*`)를 따른다.

## 7. 반응형 중단점 (Responsive Breakpoint)

| 중단점 | 폭 | 동작 |
|--------|----|----|
| Desktop | ≥1024px | 기본 레이아웃(2분할·LNB 전개) |
| Tablet | 768~1023px | 입력/프롬프트 패널 비율 유지하되 여백 축소, LNB 축소 |
| Mobile | <768px | 2분할 → 세로 1열 스택(`flex-direction: column`), 프롬프트 패널은 하단 고정 또는 접기, LNB → 햄버거 메뉴 |

모바일 전환 시 랜딩의 2분할 모드 카드는 세로 1열로 스택되어 화면 깨짐을 방지한다(NFR-LND-020). 터치 타깃은 최소 44×44px를 보장한다.

## 8. 성능·접근성 가이드

랜딩 페이지는 최초 진입 시 화면 인터랙션 가능 시간(TTI)을 1.5초 이내로 보장하기 위해, 메인 이미지를 WebP로 인코딩하고 레이지 로딩을 적용한다(NFR-LND-010). 폰트는 시스템 폴백을 두어 로딩 지연 시에도 텍스트가 즉시 보이게 한다. 접근성을 위해 본문 대비 비율은 4.5:1 이상을 유지하고, 색에만 의존하지 않도록 호환성 상태에는 색 점과 함께 "정상/위험" 텍스트 라벨을 병기한다.

## 9. 아이콘·이미지 규칙

아이콘은 선(line) 스타일로 통일하고 굵기를 일관되게 한다. 완성 PC 대표 이미지는 동일 비율(4:3 권장)로 통일하고 WebP로 제공한다. 부품 카테고리 아이콘은 CPU·GPU·RAM·SSD·파워·보드·케이스 7종을 동일 스타일로 준비한다.

---

---

### 7.1 랜딩 확장 섹션 디자인 기준 (2026-06-19)

현재 랜딩은 히어로 이후에도 여러 전환 보조 섹션을 포함한다. 각 섹션의 디자인 기준은 다음과 같다.

| 섹션 | 디자인 기준 |
|------|-------------|
| Hero | 첫 뷰포트에서 팝콘PC AI 브랜드와 “3대 AI 병렬 분석 + 실재고 검증” 메시지가 즉시 보여야 한다. CTA는 초급자/고급자 2개를 제공한다. |
| RealtimePanel | 정보 밀도가 높은 2열 패널로 구성한다. 인기 견적과 가격 동향은 카드보다 표/리스트에 가깝게 스캔 가능해야 한다. |
| Mode Select | 초급자 카드는 밝은 표면, 고급자 카드는 어두운 기술 톤을 사용해 사용자 수준 차이를 시각적으로 구분한다. |
| How It Works | 4단계 탭과 도트 인디케이터를 사용해 사용 흐름을 짧게 설명한다. |
| Spec Preview | 부품 카테고리별 컬러 배지와 짧은 모델명을 사용한다. 실제 추천 결과처럼 보이되 과도한 설명 문구는 피한다. |
| AI Engines | Gemini, ChatGPT, Claude를 각각 고유 색으로 구분한다. Fail-safe 안내는 보안/안정성 메시지로 별도 박스 처리한다. |
| Reviews | 후기 카드는 3열을 기본으로 하며, 모바일에서는 1열 스택을 허용한다. |
| Special Event Banner | 이벤트/특가 정보는 롤링 배너 형태로 노출하되, CTA 흐름을 방해하지 않도록 본문 섹션 사이에 배치한다. |

## 10. 관리자 V3.0 디자인 기준

관리자 화면은 V3.0부터 비용 관제 중심이 아니라 비즈니스 운영 중심으로 재정렬한다. 따라서 시각적 우선순위는 다음 순서로 둔다.

1. 실시간 견적·매출·마케팅 지표
2. 상품 마스터 및 재고 제어
3. 가격 정책 및 추천 가중치
4. 사용자 행동 분석
5. 시스템 비용 및 인프라 제어
6. 운영자 및 권한 관리

### 10.1 adm-dashboard

`adm-dashboard`는 운영자 메인 컨트롤 타워로, 첫 화면에서 다음 UI가 순서대로 보여야 한다.

| 영역 | 디자인 기준 |
|------|-------------|
| 실시간 AI 최종 견적 피드 | 타임라인 또는 리스트 카드 형태. 초급자/고급자 모드 배지를 구분하고, 최종 견적 금액을 Number 스타일로 강조한다. |
| 금주 특가상품 CTR | Recharts 바 차트 사용. 클릭률이 높은 상품은 Primary 색, 전주 대비 하락 항목은 Warning 또는 Error 보조 표시를 사용한다. |
| 유저 자연어 키워드 TOP 10 | 랭킹 리스트 또는 워드클라우드. 급상승 키워드는 작은 상승 배지로 표시한다. |
| 부품 가격동향 및 클릭분포 | 라인 차트와 분포형 바 차트를 조합한다. 공급가 변동과 사용자 관심 가격대를 함께 비교한다. |
| AI 비용 미니 위젯 | 우측 하단 또는 대시보드 보조 영역에 축소 배치한다. 비용이 핵심 주인공이 되지 않도록 카드 크기를 작게 유지한다. |

### 10.2 adm-product-master

상품 마스터 화면은 운영 효율성이 가장 중요하다.

| UI 요소 | 디자인 기준 |
|--------|-------------|
| 검색 필터 | 카테고리, 상태, 제조사, 키워드를 한 줄 또는 2줄 이내로 압축 배치한다. |
| 상품 리스트 | 50건/페이지 기준 테이블. 상품코드, 상품명, 카테고리, 상태, 가격, 제조사, AI 필드 상태를 표시한다. |
| 상태 토글 | `판매중`, `품절`, `단종`, `삭제대기` 상태를 명확한 컬러 배지로 표시한다. `품절`/`단종` 변경 시 확인 모달을 띄운다. |
| AI 호환성 필드 편집 | 소켓, 전력 W, 치수 mm 등 필수 필드는 테이블 인라인 편집 또는 우측 슬라이드 패널에서 수정 가능하게 한다. |
| 1초 차단 피드백 | 상태 변경 성공 시 “추천 후보군에서 제외 완료” 토스트를 즉시 표시한다. |

### 10.3 adm-csv-import

CSV 업로드 화면은 오류 행을 빠르게 파악할 수 있어야 한다.

- 업로드 전 파일명, 행 수, 예상 Insert/Update 수를 표시한다.
- 업로드 후 성공 행, 업데이트 행, 오류 행을 분리 카드로 표시한다.
- 오류 행은 다운로드 가능한 리포트 형태로 제공한다.
- `자체상품코드` 누락, 필수 가격 필드 오류, 상태값 오류는 Error 색으로 표시한다.

### 10.4 adm-price-policy / adm-recommend-weights

정책 화면은 운영자가 변경 결과를 직관적으로 이해할 수 있어야 한다.

- 카테고리별 마진율은 테이블 + 슬라이더 조합으로 제공한다.
- 추천 가중치는 재고소진, 마진극대, 가성비 3개 슬라이더 총합이 100%가 되도록 표현한다.
- 총합이 100%가 아닐 경우 저장 버튼을 Disabled 처리하거나 자동 정규화 안내를 제공한다.

### 10.5 adm-system-limit

시스템 비용 제어 화면은 V3.0에서 후순위 메뉴다.

- Rate Limit과 Circuit Breaker는 같은 화면에서 관리한다.
- 위험한 설정 변경 시 Danger 버튼 또는 확인 모달을 사용한다.
- API Key는 어떠한 경우에도 화면에 평문 노출하지 않는다.

### 10.6 adm-operators

운영자 관리 화면은 관리자 계정과 접근 권한을 다루므로 정보 밀도와 안전 장치가 중요하다.

- 운영자 목록은 테이블을 기본으로 하며 운영자, 이메일, 역할, 접근 메뉴, 상태, 마지막 로그인, 관리 액션을 표시한다.
- 역할 배지는 슈퍼관리자, 관리자, MD, 분석담당, 읽기전용을 시각적으로 구분한다.
- 초대, 권한 변경, 활성/비활성 처리는 모달에서 확인 후 적용한다.
- 슈퍼관리자 본인 계정은 비활성 처리와 권한 변경 액션을 숨기거나 비활성화한다.
- 활동 로그는 시간, 운영자명, 액션, 상세, IP 마스킹 값을 한 줄 로그 형태로 표시한다.
